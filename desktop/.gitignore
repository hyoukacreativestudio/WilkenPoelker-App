node_modules/
*.local
.env
dist-electron/
release/
# dist/ is committed on purpose: the backend serves it at /pc so every PC just
# opens a URL (no build step on the server). Rebuild with:
#   VITE_BASE=/pc/ npm run build
